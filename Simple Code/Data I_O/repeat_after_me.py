"""
Write a program that reads three lines in turn and then outputs them in the same 
sequence, each on a separate line.

Input data format
The input to the program is three lines, each on a separate line.

Output format
The program should output the entered lines in the same sequence, each on a separate line.

Note. To read text, use the input() command, to print text on the screen, use the print() command.
"""
a = input()
b = input()
c = input()
print(a)
print(b)
print(c)