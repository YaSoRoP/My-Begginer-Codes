"""
Write a program that reads three lines in turn and then outputs them in reverse order, 
each on a separate line.

Input data format
The input to the program is three lines, each on a separate line.

Output format
The program should output the entered lines in reverse order, each on a separate line.

Note. Use 3 variables to store entered lines of text.
"""
a = input()
b = input()
c = input()
print(c)
print(b)
print(a)